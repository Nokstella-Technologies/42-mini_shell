#include "tests.h"
#include <string.h>

TEST(HANDLERS_1, test_command_order_1) {
	// t_ms *ms;
	// char *cmd ="ls | grep a";
	// char *res;
	// ms = init_struct(cmd);
	// tokeneer(ms, cmd, 0, NULL);
	// verify_next_move(ms);
	// end_program(&ms);
	// res = get_next_line(ms->fd.fd[0]);
	// EXPECT_STREQ(res, "header");
	// res = get_next_line(ms->fd.fd[0]);
	// EXPECT_STREQ(res, "Makefile");
}